---
trigger: always_on
---

- My project's backend programming language is c#
- My project's frontend programming language is javascript
- My project fronted is a spa using only plain vanilla javascript. Look into app.js, router,js, auth-utils.js and api.js
- Use early returns when possible
- Always add documentation when creating new functions and classes
- never modify or delete existing code,  Create artefacts instead
- never modify, move or delete a file.  Create artefacts instead
- never create a new file. Create artefacts instead
- use existing translation features of the project. Always avoid using clear text
- use the project existing css and us rules. If not sure take a look at existing views
- javascript generated views must be csp conform
- When creating JavaScript files, make sure they are typed. If types are missing, add them to /wwwroot/js/types.d.js